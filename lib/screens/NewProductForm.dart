import 'package:flutter/material.dart';

class NewProductForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("NewProductForm"),
      ),
    );
  }
}
